if (ch == " ") return(" ")
ascii <- utf8ToInt(ch)
shifted <- ((ascii - utf8ToInt("A") + 3) %% 26) + utf8ToInt("A")
intToUtf8(shifted)
}
shifted_msg <- paste0(sapply(strsplit(msg, NULL)[[1]], shift_char), collapse = "")
# reverse
reversed_msg <- paste0(rev(strsplit(shifted_msg, NULL)[[1]]), collapse = "")
# replace spaces with asterix
asterix_msg <- gsub(" ", "*", reversed_msg)
# lowercase letters at even indices (0-indexed)
chars <- strsplit(asterix_msg, NULL)[[1]]
styled_chars <- sapply(seq_along(chars), function(i) {
if ((i-1) %% 2 == 0) {
tolower(chars[i])
} else {
toupper(chars[i])
}
})
styled_msg <- paste0(styled_chars, collapse = "")
# add marker
encoded_msg <- paste0('"\\#\\#\\#', styled_msg, '#\\#\\#\\"')
return(encoded_msg)
}
# Assume msg_to_decode is the encoded message
msg_to_decode <- encode_message(msg1)
decode_message <- function(msg) {
# remove markers
cleaned_msg <- gsub('^"\\\\#\\\\#\\\\#', '', msg)
# Remove trailing \#\#\# + quote
cleaned_msg <- gsub('\\\\#\\\\#\\\\#"$', '', cleaned_msg)
cat("prev:", msg, "\n")
cat("cleaned:", cleaned_msg, "\n")
# uppercase all
unstyled_msg <- toupper(cleaned_msg)
# astrerix instead of spaces
remove_asterix_msg <- gsub("\\*", " ", unstyled_msg)
# reverse string
unreversed_msg <- paste0(rev(strsplit(remove_asterix_msg, NULL)[[1]]), collapse = "")
# unshift
unshift_char <- function(ch) {
if (ch == " ") return(" ")
ascii <- utf8ToInt(ch)
shifted <- ((ascii - utf8ToInt("A") - 3) %% 26) + utf8ToInt("A")
intToUtf8(shifted)
}
decoded_msg <- paste0(sapply(strsplit(unreversed_msg, NULL)[[1]], unshift_char), collapse = "")
return(decoded_msg)
}
# Example message
cat("Original message:", msg1, "\n")
# Encode the message
encoded_output <- encode_message(msg1)
cat("Encoded message:", encoded_output, "\n")
# Decode the message
decoded_output <- decode_message(msg_to_decode)
cat("Decoded message:", decoded_output, "\n")
# Function to check if the message is valid from A without print statemets
is_valid_message <- function(msg) {
msg_to_encode <- gsub(" ", "", msg)
return(grepl("^[A-Z]+$", msg_to_encode))
}
# Encoding function
encode_message <- function(msg) {
if (!is_valid_message(msg)) {
stop("Message must contain only uppercase letters and spaces.")
}
#shift by 3
shift_char <- function(ch) {
if (ch == " ") return(" ")
ascii <- utf8ToInt(ch)
shifted <- ((ascii - utf8ToInt("A") + 3) %% 26) + utf8ToInt("A")
intToUtf8(shifted)
}
shifted_msg <- paste0(sapply(strsplit(msg, NULL)[[1]], shift_char), collapse = "")
# reverse
reversed_msg <- paste0(rev(strsplit(shifted_msg, NULL)[[1]]), collapse = "")
# replace spaces with asterix
asterix_msg <- gsub(" ", "*", reversed_msg)
# lowercase letters at even indices (0-indexed)
chars <- strsplit(asterix_msg, NULL)[[1]]
styled_chars <- sapply(seq_along(chars), function(i) {
if ((i-1) %% 2 == 0) {
tolower(chars[i])
} else {
toupper(chars[i])
}
})
styled_msg <- paste0(styled_chars, collapse = "")
# add marker
encoded_msg <- paste0('"\\#\\#\\#', styled_msg, '#\\#\\#\\"')
return(encoded_msg)
}
# Assume msg_to_decode is the encoded message
msg_to_decode <- encode_message(msg1)
decode_message <- function(msg) {
# remove markers
cleaned_msg <- gsub('"\\\\#\\\\#\\\\#', '', msg)
# Remove trailing \#\#\# + quote
cleaned_msg <- gsub('\\\\#\\\\#\\\\#"', '', cleaned_msg)
cat("prev:", msg, "\n")
cat("cleaned:", cleaned_msg, "\n")
# uppercase all
unstyled_msg <- toupper(cleaned_msg)
# astrerix instead of spaces
remove_asterix_msg <- gsub("\\*", " ", unstyled_msg)
# reverse string
unreversed_msg <- paste0(rev(strsplit(remove_asterix_msg, NULL)[[1]]), collapse = "")
# unshift
unshift_char <- function(ch) {
if (ch == " ") return(" ")
ascii <- utf8ToInt(ch)
shifted <- ((ascii - utf8ToInt("A") - 3) %% 26) + utf8ToInt("A")
intToUtf8(shifted)
}
decoded_msg <- paste0(sapply(strsplit(unreversed_msg, NULL)[[1]], unshift_char), collapse = "")
return(decoded_msg)
}
# Example message
cat("Original message:", msg1, "\n")
# Encode the message
encoded_output <- encode_message(msg1)
cat("Encoded message:", encoded_output, "\n")
# Decode the message
decoded_output <- decode_message(msg_to_decode)
cat("Decoded message:", decoded_output, "\n")
# Function to check if the message is valid from A without print statemets
is_valid_message <- function(msg) {
msg_to_encode <- gsub(" ", "", msg)
return(grepl("^[A-Z]+$", msg_to_encode))
}
# Encoding function
encode_message <- function(msg) {
if (!is_valid_message(msg)) {
stop("Message must contain only uppercase letters and spaces.")
}
#shift by 3
shift_char <- function(ch) {
if (ch == " ") return(" ")
ascii <- utf8ToInt(ch)
shifted <- ((ascii - utf8ToInt("A") + 3) %% 26) + utf8ToInt("A")
intToUtf8(shifted)
}
shifted_msg <- paste0(sapply(strsplit(msg, NULL)[[1]], shift_char), collapse = "")
# reverse
reversed_msg <- paste0(rev(strsplit(shifted_msg, NULL)[[1]]), collapse = "")
# replace spaces with asterix
asterix_msg <- gsub(" ", "*", reversed_msg)
# lowercase letters at even indices (0-indexed)
chars <- strsplit(asterix_msg, NULL)[[1]]
styled_chars <- sapply(seq_along(chars), function(i) {
if ((i-1) %% 2 == 0) {
tolower(chars[i])
} else {
toupper(chars[i])
}
})
styled_msg <- paste0(styled_chars, collapse = "")
# add marker
encoded_msg <- paste0('"\\#\\#\\#', styled_msg, '#\\#\\#\\"')
return(encoded_msg)
}
# Assume msg_to_decode is the encoded message
msg_to_decode <- encode_message(msg1)
decode_message <- function(msg) {
# remove markers
cleaned_msg <- substr(msg, 6, nchar(msg) - 5)
cat("prev:", msg, "\n")
cat("cleaned:", cleaned_msg, "\n")
# uppercase all
unstyled_msg <- toupper(cleaned_msg)
# astrerix instead of spaces
remove_asterix_msg <- gsub("\\*", " ", unstyled_msg)
# reverse string
unreversed_msg <- paste0(rev(strsplit(remove_asterix_msg, NULL)[[1]]), collapse = "")
# unshift
unshift_char <- function(ch) {
if (ch == " ") return(" ")
ascii <- utf8ToInt(ch)
shifted <- ((ascii - utf8ToInt("A") - 3) %% 26) + utf8ToInt("A")
intToUtf8(shifted)
}
decoded_msg <- paste0(sapply(strsplit(unreversed_msg, NULL)[[1]], unshift_char), collapse = "")
return(decoded_msg)
}
# Example message
cat("Original message:", msg1, "\n")
# Encode the message
encoded_output <- encode_message(msg1)
cat("Encoded message:", encoded_output, "\n")
# Decode the message
decoded_output <- decode_message(msg_to_decode)
cat("Decoded message:", decoded_output, "\n")
# Function to check if the message is valid from A without print statemets
is_valid_message <- function(msg) {
msg_to_encode <- gsub(" ", "", msg)
return(grepl("^[A-Z]+$", msg_to_encode))
}
# Encoding function
encode_message <- function(msg) {
if (!is_valid_message(msg)) {
stop("Message must contain only uppercase letters and spaces.")
}
#shift by 3
shift_char <- function(ch) {
if (ch == " ") return(" ")
ascii <- utf8ToInt(ch)
shifted <- ((ascii - utf8ToInt("A") + 3) %% 26) + utf8ToInt("A")
intToUtf8(shifted)
}
shifted_msg <- paste0(sapply(strsplit(msg, NULL)[[1]], shift_char), collapse = "")
# reverse
reversed_msg <- paste0(rev(strsplit(shifted_msg, NULL)[[1]]), collapse = "")
# replace spaces with asterix
asterix_msg <- gsub(" ", "*", reversed_msg)
# lowercase letters at even indices (0-indexed)
chars <- strsplit(asterix_msg, NULL)[[1]]
styled_chars <- sapply(seq_along(chars), function(i) {
if ((i-1) %% 2 == 0) {
tolower(chars[i])
} else {
toupper(chars[i])
}
})
styled_msg <- paste0(styled_chars, collapse = "")
# add marker
encoded_msg <- paste0('"\\#\\#\\#', styled_msg, '#\\#\\#\\"')
return(encoded_msg)
}
# Assume msg_to_decode is the encoded message
msg_to_decode <- encode_message(msg1)
decode_message <- function(msg) {
# remove markers
cleaned_msg <- substr(msg, 8, nchar(msg) - 7)
cat("prev:", msg, "\n")
cat("cleaned:", cleaned_msg, "\n")
# uppercase all
unstyled_msg <- toupper(cleaned_msg)
# astrerix instead of spaces
remove_asterix_msg <- gsub("\\*", " ", unstyled_msg)
# reverse string
unreversed_msg <- paste0(rev(strsplit(remove_asterix_msg, NULL)[[1]]), collapse = "")
# unshift
unshift_char <- function(ch) {
if (ch == " ") return(" ")
ascii <- utf8ToInt(ch)
shifted <- ((ascii - utf8ToInt("A") - 3) %% 26) + utf8ToInt("A")
intToUtf8(shifted)
}
decoded_msg <- paste0(sapply(strsplit(unreversed_msg, NULL)[[1]], unshift_char), collapse = "")
return(decoded_msg)
}
# Example message
cat("Original message:", msg1, "\n")
# Encode the message
encoded_output <- encode_message(msg1)
cat("Encoded message:", encoded_output, "\n")
# Decode the message
decoded_output <- decode_message(msg_to_decode)
cat("Decoded message:", decoded_output, "\n")
# Function to check if the message is valid from A without print statemets
is_valid_message <- function(msg) {
msg_to_encode <- gsub(" ", "", msg)
return(grepl("^[A-Z]+$", msg_to_encode))
}
# Encoding function
encode_message <- function(msg) {
if (!is_valid_message(msg)) {
stop("Message must contain only uppercase letters and spaces.")
}
#shift by 3
shift_char <- function(ch) {
if (ch == " ") return(" ")
ascii <- utf8ToInt(ch)
shifted <- ((ascii - utf8ToInt("A") + 3) %% 26) + utf8ToInt("A")
intToUtf8(shifted)
}
shifted_msg <- paste0(sapply(strsplit(msg, NULL)[[1]], shift_char), collapse = "")
# reverse
reversed_msg <- paste0(rev(strsplit(shifted_msg, NULL)[[1]]), collapse = "")
# replace spaces with asterix
asterix_msg <- gsub(" ", "*", reversed_msg)
# lowercase letters at even indices (0-indexed)
chars <- strsplit(asterix_msg, NULL)[[1]]
styled_chars <- sapply(seq_along(chars), function(i) {
if ((i-1) %% 2 == 0) {
tolower(chars[i])
} else {
toupper(chars[i])
}
})
styled_msg <- paste0(styled_chars, collapse = "")
# add marker
encoded_msg <- paste0('"\\#\\#\\#', styled_msg, '#\\#\\#\\"')
return(encoded_msg)
}
# Assume msg_to_decode is the encoded message
msg_to_decode <- encode_message(msg1)
decode_message <- function(msg) {
# remove markers, sub failed so resorted to this method
cleaned_msg <- substr(msg, 8, nchar(msg) - 7)
# uppercase all
unstyled_msg <- toupper(cleaned_msg)
# astrerix instead of spaces
remove_asterix_msg <- gsub("\\*", " ", unstyled_msg)
# reverse string
unreversed_msg <- paste0(rev(strsplit(remove_asterix_msg, NULL)[[1]]), collapse = "")
# unshift
unshift_char <- function(ch) {
if (ch == " ") return(" ")
ascii <- utf8ToInt(ch)
shifted <- ((ascii - utf8ToInt("A") - 3) %% 26) + utf8ToInt("A")
intToUtf8(shifted)
}
decoded_msg <- paste0(sapply(strsplit(unreversed_msg, NULL)[[1]], unshift_char), collapse = "")
return(decoded_msg)
}
# Example message
cat("Original message:", msg1, "\n")
# Encode the message
encoded_output <- encode_message(msg1)
cat("Encoded message:", encoded_output, "\n")
# Decode the message
decoded_output <- decode_message(msg_to_decode)
cat("Decoded message:", decoded_output, "\n")
# Function to check if the message is valid from A without print statemets
is_valid_message <- function(msg) {
msg_to_encode <- gsub(" ", "", msg)
return(grepl("^[A-Z]+$", msg_to_encode))
}
# Encoding function
encode_message <- function(msg) {
if (!is_valid_message(msg)) {
stop("Message must contain only uppercase letters and spaces.")
}
#shift by 3
shift_char <- function(ch) {
if (ch == " ") return(" ")
ascii <- utf8ToInt(ch)
shifted <- ((ascii - utf8ToInt("A") + 3) %% 26) + utf8ToInt("A")
intToUtf8(shifted)
}
shifted_msg <- paste0(sapply(strsplit(msg, NULL)[[1]], shift_char), collapse = "")
# reverse
reversed_msg <- paste0(rev(strsplit(shifted_msg, NULL)[[1]]), collapse = "")
# replace spaces with asterix
asterix_msg <- gsub(" ", "*", reversed_msg)
# lowercase letters at even indices (0-indexed)
chars <- strsplit(asterix_msg, NULL)[[1]]
styled_chars <- sapply(seq_along(chars), function(i) {
if ((i-1) %% 2 == 0) {
tolower(chars[i])
} else {
toupper(chars[i])
}
})
styled_msg <- paste0(styled_chars, collapse = "")
# add marker
encoded_msg <- paste0('"\\#\\#\\#', styled_msg, '\\#\\#\\#"')
return(encoded_msg)
}
# Assume msg_to_decode is the encoded message
msg_to_decode <- encode_message(msg1)
decode_message <- function(msg) {
# remove markers, sub failed so resorted to this method
cleaned_msg <- substr(msg, 8, nchar(msg) - 7)
# uppercase all
unstyled_msg <- toupper(cleaned_msg)
# astrerix instead of spaces
remove_asterix_msg <- gsub("\\*", " ", unstyled_msg)
# reverse string
unreversed_msg <- paste0(rev(strsplit(remove_asterix_msg, NULL)[[1]]), collapse = "")
# unshift
unshift_char <- function(ch) {
if (ch == " ") return(" ")
ascii <- utf8ToInt(ch)
shifted <- ((ascii - utf8ToInt("A") - 3) %% 26) + utf8ToInt("A")
intToUtf8(shifted)
}
decoded_msg <- paste0(sapply(strsplit(unreversed_msg, NULL)[[1]], unshift_char), collapse = "")
return(decoded_msg)
}
# Example message
cat("Original message:", msg1, "\n")
# Encode the message
encoded_output <- encode_message(msg1)
cat("Encoded message:", encoded_output, "\n")
# Decode the message
decoded_output <- decode_message(msg_to_decode)
cat("Decoded message:", decoded_output, "\n")
words <- unlist(strsplit(msg2, " "))
# Find the index of the first occurrence of "SPY"
index <- which(words == "SPY")[1]  # [1] in case it occurs multiple times
# Print the result
cat("The word index of the first occurrence of the keyword SPY is:", index, "\n")
words <- unlist(strsplit(msg2, " "))
index <- which(words == "SPY")[1]
cat("The word index of the first occurrence of the keyword SPY is:", index, "\n")
letters <- gsub(" ", "", ,msg3)
letters <- gsub(" ", "", msg3)
l_vec <- unlist(strsplit(letters, " "))
l_freq <- table(factor(l_vec, levels = letters))
print(l_freq)
letters <- gsub(" ", "", msg3)
l_vec <- unlist(strsplit(letters, " "))
l_freq <- table(factor(l_vec, levels = letters))
print(l_freq)
barplot(letter_freq)
letters <- gsub(" ", "", msg3)
l_vec <- unlist(strsplit(letters, " "))
l_freq <- table(factor(l_vec, levels = letters))
print(l_freq)
barplot(l_freq)
letters <- gsub("*", "", msg3)
l_vec <- unlist(strsplit(letters, " "))
l_freq <- table(factor(l_vec, levels = letters))
print(l_freq)
barplot(l_freq)
letters <- gsub("\\*", "", msg3)
l_vec <- unlist(strsplit(letters, " "))
l_freq <- table(factor(l_vec, levels = letters))
print(l_freq)
barplot(l_freq)
letters <- gsub("\\*", "", msg3)
l_vec <- unlist(strsplit(letters, ""))
l_freq <- table(factor(l_vec, levels = letters))
print(l_freq)
barplot(l_freq)
letters <- gsub("\\*", "", msg3)
l_vec <- strsplit(letters, "")
l_freq <- table(factor(l_vec, levels = letters))
print(l_freq)
barplot(l_freq)
View(l_vec)
letters <- gsub("\\*", "", msg3)
l_vec <- strsplit(letters, "")
unlist <- unlist(l_vec)
l_freq <- table(factor(unlist, levels = letters))
print(l_freq)
barplot(l_freq)
letters <- gsub("\\*", "", msg3)
l_vec <- strsplit(letters, "")
unlist <- unlist(l_vec)
l_freq <- table(factor(unlist, levels = letters))
print(l_freq)
barplot(unlist)
letters <- gsub("\\*", "", msg3)
l_vec <- strsplit(letters, "")
unlist <- unlist(l_vec)
l_freq <- table(unlist)
print(l_freq)
barplot(unlist)
letters <- gsub("\\*", "", msg3)
l_vec <- strsplit(letters, "")
unlist <- unlist(l_vec)
l_freq <- table(unlist)
print(l_freq)
barplot(l_freq)
letters <- gsub("\\*", "", msg3)
l_vec <- strsplit(letters, "")
unlist <- unlist(l_vec)
table <- factor(unlist, levels = LETTERS)
l_freq <- table(unlist)
print(l_freq)
barplot(l_freq)
letters <- gsub("\\*", "", msg3)
l_vec <- strsplit(letters, "")
unlist <- unlist(l_vec)
table <- factor(unlist, levels = LETTERS)
l_freq <- table(unlist)
print(table)
barplot(l_freq)
letters <- gsub("\\*", "", msg3)
l_vec <- strsplit(letters, "")
unlist <- unlist(l_vec)
table <- factor(unlist, levels = LETTERS)
l_freq <- table(table)
barplot(l_freq)
letters <- gsub("\\*", "", msg3)
l_vec <- strsplit(letters, "")
l_vec <- unlist(l_vec)
table <- factor(unlist, levels = LETTERS)
l_freq <- table(table)
barplot(l_freq)
letters <- gsub("\\*", "", msg3)
l_vec <- strsplit(letters, "")
l_vec <- unlist(l_vec)
l_table <- factor(l_vec, levels = LETTERS)
l_freq <- table(l_table)
barplot(l_freq)
words <- unlist(strsplit(msg3, "\\*"))
word_lengths <- nchar(words)
hist(word_lengths)
generate_spy_name <- function(first_name, last_name) {
first_name_two_letters <- substr(first_name, 1, 2)
last_name_two_letters <- substr(last_name, nchar(last_name) - 1, nchar(last_name))
r_number <- sample(10:99, 1)
spy_name <- paste0(first_name_two_letters, last_name_two_letters, r_number)
cat("Your Spy Name is:", spy_name, "\n")
return(spy_name)
}
generate_spy_name("Graham", "Dib")
}
generate_spy_name <- function(first_name, last_name) {
first_name_two_letters <- substr(first_name, 1, 2)
last_name_two_letters <- substr(last_name, nchar(last_name) - 1, nchar(last_name))
r_number <- sample(10:99, 1)
spy_name <- paste0(first_name_two_letters, last_name_two_letters, r_number)
cat("Your Spy Name is:", spy_name, "\n")
return(spy_name)
}
generate_spy_name("Graham", "Dib")
